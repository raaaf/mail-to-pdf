# Audit 2026-07-24 #3 (main, pre-push, commit e7e2a37)

Scope: diff f060620..HEAD (11 files): menubar-only conversion, drop shelf, settings, launch-at-login.
Pre-checks: secrets/lockfile/binary CLEAN. Effort: high. Routing: security, architecture+quality, performance, a11y+ui+ux+animation, docs+copy (SAUBER); skipped seo/typography.

## Round 1 — fixed
- [Security/Privacy, Important] Global drag monitor materialized NSURL content of unrelated drags. Fixed: readObjects gated behind .fileURL type presence, cheap type checks first; privacy note added to README.
- [Architecture] Duplicated withObservationTracking loops -> shared ConvertModel.onStateChange. Duplicated state->icon/color/title mapping across two views -> shared State.Display mapper.
- [Architecture] Shelf show/hide race during fade (new drag swallowed, orderOut under active drag) -> fadeTask + fadeState state machine with cancellation checks.
- [Performance] NSPasteboard(name: .drag) allocated per mouse-drag event -> hoisted.
- [A11y] Status icon accessibilityDescription now state-specific; settings window sized via fittingSize (was clipping risk 260 vs 320); Escape + invisible mainMenu (Cmd+W/Cmd+Q) for keyboard close; settings icon hidden, toggle hint; Ctrl+Klick opens the context menu.
- [UX] Shelf converting card gained Abbrechen; ContentView failed state gained tap-to-dismiss (parity).
- [Animation] Shelf fade easeInOut.

## Discarded (with justification)
- Task.yield ordering fragility: documented tradeoff, honest comment, no repro.
- changeCount polling IPC cost: inherent to the technique, gated correctly (once per drag session).
- button.menu refactor: manual event check works; Ctrl-click gap fixed instead.
- Menubar icons not using Display mapper: deliberate outline/template variants vs the views' .fill symbols.
- onStateChange late re-registration race, Cmd+W-vs-popover interaction: theoretical, no repro; popover behavior in manual test plan.
- Verifier claim "FadeState needs explicit Equatable to compile": factually wrong (no associated values -> synthesized), disproven by two green builds.

## Verification
- 5 fix agents (2 waves), all APPLIED. Fix-verifier on the fade state machine: logic sound (one wrong compile claim, see above). Round-2 convergence: 1 medium (dismissed as deliberate), 2 low theoretical (dismissed).
- Tests: 32/32 green. Build clean. Linter n/a. Changelog n/a.

AUDIT_STATUS: SAUBER

## Manual test plan
1. Drag from Mail: shelf appears below cursor, morphs to status card (subject shown), hides when the centered save dialog opens; no Mission Control trigger needed.
2. Shelf converting: Abbrechen works. Failed state: click dismisses (shelf and popover).
3. Rapid successive drags (during shelf fade-out): shelf reappears reliably.
4. Settings: fits content, Escape and Cmd+W close it, toggle "Bei Anmeldung starten" (verify after app lives in /Applications), VoiceOver reads state-specific status icon.
5. Popover open + Cmd+W: popover closes cleanly, reopens on click.
