# Audit 2026-07-24 (main, pre-push, initial commit b6acfbb)

Scope: entire initial commit (28 files, ~1800 lines). Effort: high (3 rounds max).
Pre-checks: secrets CLEAN, lockfile CLEAN, binary artifacts CLEAN. No dependency manifests.

## Routing

- Round 1: ran [security, architecture, performance, code_quality, a11y, ui_design, ux, animation, docs_sync, copy]; skipped [seo, typography: no web pages]. TRIAGE=FLOOR_ONLY (manual floor, no origin remote so collect-scope was empty; scope = full initial commit).
- Round 2: ran [security, architecture+code_quality] on fixed files; skipped [rest: fixes verified by dedicated fix-verifiers + full test suite].

## Round 1 — findings and resolution

### Critical (4, all fixed)
- [Architecture] ConvertModel.handle: unguarded concurrent drops corrupted shared PDFRenderer state. Fixed: pendingURLs queue + isProcessing flag.
- [Performance] EmailParser.parse ran synchronously on MainActor. Fixed: Task.detached.
- [A11y] No keyboard/VoiceOver-reachable action outside idle state. Fixed: file-pick button in all states except converting, Cmd+O shortcut.
- [A11y] State changes never announced to VoiceOver. Fixed: NSAccessibility announcements via onChange.

### Important (fixed)
- [Security] JS enabled in WKWebView rendering untrusted email HTML. Fixed: allowsContentJavaScript = false.
- [Architecture] catch duplicated fail(); HTML wrapper duplicated. Fixed (fail() call, wrapDocument extraction).
- [Quality] Silent attachment-write failures; swallowed errors without logging. Fixed (failure count in state, os.Logger).
- [Quality] No CRLF fixture despite CRLF being the primary input path. Fixed: crlf-multipart.eml + parity test.
- [Performance] base64 Data->String round-trip; splitMultipart String-per-line allocation; unconditional 1.5s image grace. Fixed (direct Data decode, byte-level scan, conditional grace via remote-content regex).
- [A11y/UX] failed state auto-vanished after 5s. Fixed: failed persists until next action; new cancelled state for save-panel cancel.
- [Docs] README understated attachment detection (.pdf filename fallback). Fixed.
- [Copy] English "attachment.pdf" fallback; dead "Anhang.pdf" fallback; "Mail" vs "E-Mail" terminology. Fixed.

### Minor (fixed)
- Multipart recursion depth cap (20). A4 constants deduplicated. Mail temp .eml cleanup (scoped to temporaryDirectory/MailToPDF-*). Cmd+O shortcut. Error messages with next step (settings path for Automation, retry hint). README documents icon script.

### Discarded (with justification)
- Remote image loading during render: deliberate feature for invoice logos, timeboxed (documented tradeoff).
- Hardened runtime off: release signing enables it (template convention, make-release.sh stage).
- Input size cap, %PDF magic check: speculative, low confidence, parser is linear.
- Spacing rhythm 14/2/2, spring-vs-easing: stylistic/idiomatic.
- CLAUDE.md command "duplication": CLAUDE.md is deliberately self-contained.
- Partial temp-write failure reporting (MailDropView): rare local-disk edge; all-fail error path exists.

## Fix verification
- 7 fix agents round 1, all APPLIED; working-tree cross-check passed (assigned files == modified files).
- 3 fix-verifiers: ConvertModel keep; EmailParser PATCH (empty-part Range crash in new splitMultipart, attacker-triggerable, fixed with clamp + regression test); PDFRenderer/ContentView PATCH (remote-content regex false negatives for url("...")/background=, fixed).
- Round 2: 1 new Important (temp PDF orphaned on error paths) fixed via hoisted tempURL + defer cleanup; security re-check SAUBER.

## Result
AUDIT_STATUS: SAUBER (after round 2 + 1 targeted fix)
Tests: 24/24 green (EmailParser 12, FilenameSanitizer 7, ConvertModel filenames 5). Build clean.
Linter: n/a (no linter configured). Changelog: n/a (initial release).

## Open points (user decision, Phase 3f)
User chose "fix now" for both; both implemented and verified (24/24 tests):
- Cancel affordance during .converting: ConvertModel.cancel() + queue clearing + cancellation checkpoints, "Abbrechen" button with Esc shortcut.
- Larger-Text layout: ViewThatFits with ScrollView fallback around the state content.

## Manual test plan (visual changes)
1. Drop a Mail message: pulsing state, save dialog, green check for 3s, back to idle.
2. Cancel the save dialog: "Abgebrochen" state, auto-reset after 3s.
3. Drop a broken file (rename a .txt to .eml with garbage): failed state persists, button remains usable, Cmd+O works.
4. Drop 3 selected messages at once: sequential processing, one save dialog each, no hang.
5. VoiceOver on: state changes are announced.
